#include <stdio.h>

int main(int argc, char *argv[]) {
	char adjective[] = "cruel";
	char noun[] = "world";
	printf("Goodbye, %s %s!\n", adjective, noun);
	puts("Puts does not accept or support format specifiers.\n");
	return (0);
}
